# Internal build-planning / agent-instruction docs — not published course content
CLAUDE.md
COURSE_PLAN.md

# Secrets
.env

# Local tooling state
.claude/

# Python / Jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/

# Every database the 7-day course uses lives in Databases/. Datasets are committed
# directly when small enough so students don't need a separate download step; only
# exclude a specific oversized file here when it actually can't fit in git.

# Day 06's SQLite file is generated by running day06_python_sqlite.ipynb
# (migrated live from MySQL) — regenerate rather than commit, same reasoning
# as never committing .env.
Databases/rawaj.sqlite

# Material parked for a later course expansion, not part of the current 7-day
# lineup — kept on disk, not published.
future-improvements/
