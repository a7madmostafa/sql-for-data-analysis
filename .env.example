# Copy this file to .env (untracked by git — see .gitignore) and fill in your
# own local MySQL credentials. Used from Day 05 onward to connect from Python
# instead of hardcoding a password directly in a notebook.

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=parch_and_posey
