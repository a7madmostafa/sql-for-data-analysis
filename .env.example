# Copy this file to .env (untracked by git — see .gitignore) and fill in your
# own local MySQL password. Host/port/user/database are plain, non-secret
# values hardcoded directly in the notebooks (localhost:3306, root, rawaj) —
# only the password needs to stay out of git.

DB_PASSWORD=your_password_here
