# Day 05 — Window Functions & Stored Procedures

Still on the **Rawaj** database from Days 01–04 (no new setup script here) — this is where
you rank and total rows without collapsing them (window functions), then save reusable logic inside
the database itself (stored procedures). The last core-SQL day before Day 06 applies all of it from
Python.

## What you'll learn

- Compute a running total, rank, or row-to-row comparison with a window function, without collapsing rows the way `GROUP BY` does.
- Reset a window per group with `PARTITION BY`, and tell `RANK`/`DENSE_RANK`/`ROW_NUMBER` apart by how each handles ties.
- Look at a neighboring row with `LAG`/`LEAD`, for month-over-month style comparisons.
- Write, and `CALL`, a stored procedure with `IN` and `OUT` parameters, using `DELIMITER` correctly.

## Files

| File | What it is |
|---|---|
| `day05_reading.html` | Start here. Concepts, diagrams, and worked examples — open directly in a browser. |
| `day05_window_functions_and_stored_procedures.sql` | The annotated walkthrough — run it yourself, alongside the reading. |
| `day05_window_functions_and_stored_procedures.html` | The same walkthrough, already run — every statement plus its real result set, rendered as a page (generated by `_tools/sql_to_html.py`). No MySQL setup needed; open it to check your own output against, or read through before you have the database loaded. |
| `day05_exercises.sql` | Practice questions, business-framed. Try these before looking at the solutions. |
| `day05_exercises_solutions.sql` | Answer key, numbered to match the exercises. |

**Before you start:** run `../Databases/rawaj_db.sql` if you haven't already.

**Order:** `day05_reading.html` → `day05_window_functions_and_stored_procedures.sql` → `day05_exercises.sql`.
